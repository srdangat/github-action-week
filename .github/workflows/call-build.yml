name: call build workflw

on:
  push:
    branches: [main]

jobs:
  build:
    uses: ./.github/workflows/reusable-build.yml
    with:
      app_name: "web-app"
      environment: "staging"
    secrets:
      docker_token: ${{ secrets.DOCKER_TOKEN }}
